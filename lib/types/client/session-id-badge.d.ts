/**
 * Session ID badge — displays the short session ID (first 8 chars)
 * as a chip in the session header actions list.
 */
import React from 'react';
export declare function SessionIdBadge(props: {
    sessionId?: string;
}): React.JSX.Element | null;
